{
  "name": "flower",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run db:generate:auth && pnpm run cf:types && pnpm run typecheck && nuxt build",
    "dev": "nuxt dev",
    "dev:systemd": "systemd-run --user --scope -p MemoryMax=4096M nuxt dev",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "nuxt typecheck",
    "clean": "rm -rf .output .wrangler .nuxt node_modules",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:generate:auth": "pnpm better-auth generate --config auth.config.ts --output server/db/auth.ts -y && eslint --fix server/db/auth.ts",
    "db:generate": "pnpm run db:generate:auth && dotenv -e .env drizzle-kit generate",
    "db:migrate": "pnpm run cf:migrate",
    "cf:deploy": "pnpm run build && wrangler --cwd .output deploy",
    "cf:types": "wrangler types shared/wrangler.d.ts && eslint --fix shared/wrangler.d.ts",
    "cf:migrate": "wrangler d1 migrations apply flower",
    "cf:secrets": "wrangler secret bulk .env"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.974.0",
    "@aws-sdk/lib-storage": "^3.974.0",
    "@aws-sdk/s3-request-presigner": "^3.974.0",
    "@langchain/cloudflare": "^1.0.1",
    "@langchain/core": "^1.1.16",
    "@nuxt/eslint": "1.12.1",
    "@nuxt/ui": "^4.4.0",
    "@trpc/client": "^11.8.1",
    "@trpc/server": "^11.8.1",
    "@vueuse/core": "^14.1.0",
    "@vueuse/nuxt": "^14.1.0",
    "better-auth": "^1.4.17",
    "drizzle-orm": "^0.45.1",
    "nuxt": "^4.3.0",
    "trpc-nuxt": "^2.0.1",
    "vue": "^3.5.27",
    "vue-router": "^4.6.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.4.17",
    "@iconify-json/lucide": "^1.2.86",
    "@types/node": "^25.0.10",
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "^0.31.8",
    "eslint": "^9.39.2",
    "nitro-cloudflare-dev": "^0.2.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vue-tsc": "^3.2.2",
    "wrangler": "^4.60.0"
  }
}